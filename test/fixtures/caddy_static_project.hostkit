use HostKit.DSL, providers: [HostKit.Providers.Caddy]

project :caddy_static_demo do
  service :landing do
    caddy_site :landing, "elixir.toys", path: "landing.caddy" do
      root "/srv/toys/www/elixir.toys"
      encode [:zstd, :gzip]
      file_server()
    end
  end
end
